<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Navibus" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.Navibus" parent="Base.Theme.Navibus" />

    <!-- Style for the dropdown menu -->
    <style name="AppTheme.Dropdown" parent="ThemeOverlay.Material3.Dark">
        <item name="android:colorBackground">@color/card_background</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>